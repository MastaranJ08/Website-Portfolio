# Backend environment variables for the AI studio
# Keep API keys server-side only. Never expose them in frontend code.
NVIDIA_API_KEY=your_nvidia_api_key_here

# Optional overrides for providers / proxies
NVIDIA_API_URL=https://integrate.api.nvidia.com/v1
NVIDIA_MODEL=z-ai/glm-5.1

PORT=3001
